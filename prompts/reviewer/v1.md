# 题面全文（statement.md）
```markdown
{{ statement_markdown }}
```

# 形式化定义（spec，事实基准）
任务：{{ task }}
输入格式：{{ input_format }}
输出格式：{{ output_format }}
约束：{{ constraints_items }}
样例数：{{ sample_count }}

# 系统确定性检查结果
{{ deterministic_checks }}

请独立审查题面文本质量与一致性。

请输出 JSON（不要输出其他内容）：
{
  "passed": true/false,
  "issues": ["问题1", "问题2"],
  "notes": "总体评价"
}
