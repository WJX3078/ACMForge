# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}

## gen.py 可用模式
{{ modes }}

## 已知错误解法（测试集必须全部击杀它们；target_mutants 必须引用下面的 id）
{% for m in mutants %}
- **{{ m.id }}** [{{ m.category }}] {{ m.description }}（预期 {{ m.expected_verdict }}，来源 {{ m.origin }}）
{% endfor %}

## 标准解法思路
{% for a in intended.algorithm %}
- {{ a }}{% endfor %}

请设计测试策略：min/max/random 必须有；针对上面每个错误解法设计至少一条对抗策略；
随机类策略用 count 表示生成几组（seed 会自动分配），一般 count=3~8。

请输出 JSON（不要输出其他内容）：
{
  "strategies": [
    {
      "name": "策略名（英文小写下划线）",
      "purpose": "针对什么/为什么需要",
      "mode": "gen.py 模式名",
      "params": {"可选参数如 n": 0},
      "count": 1,
      "priority": 100,
      "target_mutants": ["定向针对的变异体 id，可留空"]
    }
  ]
}
