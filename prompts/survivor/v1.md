# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}

## gen.py 可用模式（mode 只能从中选择）
{{ modes }}

## 已有的测试策略
{{ strategies }}

## 幸存的错误解（测试集未能击杀它们）
{% for s in survivors %}
### {{ s.id }}（{{ s.category }}，预期 {{ s.expected_verdict }}）
- 思路：{{ s.description }}
- 在评估中全部通过（未出现任何非 AC 判定）
{% endfor %}

请逐个分析幸存原因，并给出定向测试策略。

请输出 JSON（不要输出其他内容）：
{
  "analyses": [
    {
      "target_mutant_id": "幸存者 id",
      "why_survived": "为什么现有测试杀不死它（盲区在哪）",
      "required_structure": "击杀它需要的输入结构",
      "required_scale": "需要的规模",
      "required_edge_case": "需要的边界情况",
      "mode": "gen.py 模式名",
      "generator_parameters": {"如 n": 0},
      "purpose": "这条策略的说明"
    }
  ]
}
