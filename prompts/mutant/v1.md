# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 输出格式
{{ output_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}
时间限制：{{ limits.time_ms }} ms

## 标准解法（供参考，不要原样提交）
```cpp
{{ std_code }}
```

## 预期解法
算法：{% for a in intended.algorithm %}
- {{ a }}{% endfor %}

请编写 {{ count }} 个**不同的**错误解法，每个都是完整可编译的 C++20 程序。

请输出 JSON（不要输出其他内容）：
{
  "mutants": [
    {
      "code": "完整 C++20 代码",
      "category": "WRONG_GREEDY|WRONG_TRANSITION|BOUNDARY|OVERFLOW|MISSING_CASE|TLE|IMPLEMENTATION_BUG",
      "description": "错在哪里、为什么看起来有道理",
      "expected_verdict": "WA 或 TLE"
    }
  ]
}
