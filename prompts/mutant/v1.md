# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 输出格式
{{ output_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}
时间限制：{{ limits.time_ms }} ms

## 标准解法（供参考，不要原样提交）
```cpp
{{ std_code }}
```

# 你要实现的错误思路（已由出题人设计好，必须严格遵循）
- id：{{ idea.id }}
- 类别：{{ idea.category }}
- 标题：{{ idea.title }}
- 推理过程：{{ idea.reasoning_summary }}
- 为什么看起来有道理：{{ idea.why_plausible }}
- 预期复杂度：{{ idea.claimed_complexity }}
- 预期出错场景：{{ idea.expected_failure_patterns | join('；') }}
- 反例形态：{{ idea.counterexample_shape }}

# 硬性规则
1. 严格按上述错误思路实现完整可编译的 C++20 单文件程序，输入输出格式与题面一致。
2. 程序必须能正常运行（不是崩溃/编译错误），错误要真实存在于算法逻辑中。
3. 不要输出与 std 等价的代码。
4. 输出会被机器解析为 JSON，不要输出其他内容。

请输出 JSON（不要输出其他内容）：
{
  "code": "完整 C++20 代码",
  "description": "按错误思路实现的一句话说明",
  "expected_verdict": "WA 或 TLE"
}
