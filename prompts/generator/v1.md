请为下面这道题编写确定性数据生成器 gen.py。

# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}
机器可读边界（生成器必须严格遵守）：
{{ bounds }}

## 预期解法与对抗点（worst-case 设计参考）
关键观察：{% for o in intended.observations %}
- {{ o }}{% endfor %}
算法步骤：{% for a in intended.algorithm %}
- {{ a }}{% endfor %}

要求：
- 模式 "min"：最小合法输入；
- 模式 "random"：参数 --n 控制规模，取值随机；
- 模式 "max"：n 取约束允许的最大值，取值取满边界；
- 再补充至少 4 个针对本题的对抗模式（全相同/全负/极值/交替/重复/最坏结构等，按题目特点选择）。

请输出 JSON（不要输出其他内容）：
{
  "code": "完整 gen.py 源码（#!/usr/bin/env python3 开头）",
  "modes": ["min", "random", "max", "..."],
  "notes": "一句话说明各模式的对抗意图"
}
