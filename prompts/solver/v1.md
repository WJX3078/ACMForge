请为下面这道题实现标准解法（STD）。

# 题目
标题：{{ title }}
概述：{{ summary }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 输出格式
{{ output_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}
时间限制：{{ limits.time_ms }} ms；内存限制：{{ limits.memory_mb }} MB

## 预期解法（必须按此意图实现）
关键观察：{% for o in intended.observations %}
- {{ o }}{% endfor %}
算法步骤：{% for a in intended.algorithm %}
- {{ a }}{% endfor %}
预期复杂度：{{ intended.complexity.time }} 时间 / {{ intended.complexity.memory }} 内存

## 样例（仅用于理解题意，正式验证由系统自动完成）
{% for s in samples %}
输入：
```
{{ s.input }}
```
{% if s.expected_output %}输出：
```
{{ s.expected_output }}
```
{% endif %}{% endfor %}
{% if notes %}# 其他说明
{{ notes }}{% endif %}

{% if counterexample %}
# ⚠️ 修复任务
你的上一版程序在下面的反例上输出了错误结果。请找出根因并修复，输出**完整的新版本代码**（不是 diff）。

反例输入：
```
{{ counterexample.input }}
```
你的程序输出：
```
{{ counterexample.std_out }}
```
正确答案（暴力程序输出）：
```
{{ counterexample.brute_out }}
```
{% endif %}
{% if previous_code %}
# 上一版代码（{{ previous_version }}）
```cpp
{{ previous_code }}
```
{% endif %}

请输出 JSON（不要输出其他内容）：
{
  "code": "完整 C++20 代码",
  "idea_summary": "一句话说明算法思路",
  "complexity": {"time": "如 O(n log n)", "memory": "如 O(n)"}
}
