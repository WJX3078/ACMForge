# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 输出格式
{{ output_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}
时间限制：{{ limits.time_ms }} ms

## 标准解法（供参考，你的思路必须与它有本质区别）
```cpp
{{ std_code }}
```

## 预期解法
算法：{% for a in intended.algorithm %}
- {{ a }}{% endfor %}

请设计 {{ count }} 条**本质不同的错误思路**。

请输出 JSON（不要输出其他内容）：
{
  "ideas": [
    {
      "id": "小写蛇形命名",
      "category": "WRONG_GREEDY|WRONG_TRANSITION|BOUNDARY|OVERFLOW|MISSING_CASE|TLE|MLE|IMPLEMENTATION_BUG",
      "title": "一句话标题",
      "reasoning_summary": "错误思路的完整推理过程",
      "why_plausible": "为什么这个错误解看起来有道理、容易骗过选手",
      "claimed_complexity": "该思路的复杂度，如 O(n^2)",
      "expected_failure_patterns": ["什么情况下会出错"],
      "counterexample_shape": "什么样的输入会触发错误（尽量具体：规模/取值/结构）",
      "target_constraints": ["与哪些数据范围维度相关，如 n、值域"]
    }
  ]
}
