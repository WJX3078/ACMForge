# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 标准解法
思路摘要：{{ std_idea }}
关键观察：{% for o in intended.observations %}
- {{ o }}{% endfor %}
算法步骤：{% for a in intended.algorithm %}
- {{ a }}{% endfor %}
正确性证明要点：{% for p in intended.proof_outline %}
- {{ p }}{% endfor %}
复杂度：{{ intended.complexity.time }} 时间 / {{ intended.complexity.memory }} 内存

## 标准解法代码
```cpp
{{ std_code }}
```

## 真实验证数据（直接引用，不要修改）
- 对拍：{{ fuzz_cases }} 组随机小数据 std 与 brute 完全一致；反例 {{ mismatches }} 个（全部修复）。
- 测试集：{{ test_count }} 组数据，击杀错误解 {{ mutant_killed }}/{{ mutant_total }}。
- std 在最大测试上用时 {{ std_max_ms }} ms（时限 {{ time_limit_ms }} ms）。

请撰写完整题解（Markdown，可含公式）。

请输出 JSON（不要输出其他内容）：
{
  "editorial_markdown": "完整题解"
}
