请为下面这道题实现一个暴力解法（BRUTE），用于和标准解法做对拍。

# 题目
标题：{{ title }}

## 形式化任务
{{ task }}

## 输入格式
{{ input_format }}

## 输出格式
{{ output_format }}

## 约束
{% for c in constraints_items %}{{ c }}
{% endfor %}

## 样例
{% for s in samples %}
输入：
```
{{ s.input }}
```
{% if s.expected_output %}输出：
```
{{ s.expected_output }}
```
{% endif %}{% endfor %}

要求：即使复杂度高达 O(n²) 或指数级也没关系，但**答案必须绝对正确**，包括所有边界情况。

请输出 JSON（不要输出其他内容）：
{
  "code": "完整 C++20 代码",
  "approach": "一句话说明暴力方法"
}
